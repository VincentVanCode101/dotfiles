#!/bin/bash

WINDOW_NAME="Microsoft Teams"

if swaymsg "[title=\"$WINDOW_NAME\"] focus" 2>/dev/null | jq -e 'first | .success' > /dev/null 2>&1; then
  : # window found, focused
else
  google-chrome --app=https://teams.microsoft.com/ &
fi
